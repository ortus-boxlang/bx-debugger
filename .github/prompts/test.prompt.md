Open a new file and write out some content

The file will be a markdown file.

Start the document with `# ${input.title:Or Something}`

The user has provided the beginning of a knock knock joke. Consider it and generate an answer.

Put both the joke and the answer in the markdown file.